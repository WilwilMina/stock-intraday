# Base URL for the backend API. Leave empty for local dev - the Vite dev
# server proxies /api to http://localhost:3000 (see vite.config.ts), so the
# client never has to deal with CORS. Set to the deployed backend's origin
# (e.g. https://api.example.com) for a production build.
VITE_API_BASE_URL=
